---
layout: base
title: Accueil
---

<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>{{ title }}</title>
  <meta name="description" content="Simulateur de budget mariage champêtre + derniers articles du blog.">

  <!-- Favicons -->
  <link rel="icon" href="data:image/svg+xml,<svg xmlns=%27http://www.w3.org/2000/svg%27 viewBox=%270 0 100 100%27><text y=%27.9em%27 font-size=%2790%27>💍</text></svg>">
  <link rel="icon" href="/favicon.ico" sizes="any">
  <link rel="icon" href="/favicon.svg" type="image/svg+xml">
  <link rel="apple-touch-icon" href="/apple-touch-icon.png">

  <link rel="stylesheet" href="/assets/css/style.css">
</head>
<body>

<header>
  <h1>Mon Mariage Champêtre</h1>
</header>

<main>

  <!-- Simulateur -->
  <section class="simulateur card">
    <h2>Simulateur de budget mariage</h2>

    <!-- Onglets Express / Avancé -->
    <div class="tabs">
      <button id="modeExpress" class="active">Mode Express</button>
      <button id="modeAvance">Mode Avancé</button>
    </div>

    <!-- EXPRESS -->
    <div id="express">
      <label>Nombre d'invités</label>
      <div id="guestsPills" class="pills">
        <button type="button" data-val="50">50</button>
        <button type="button" data-val="100">100</button>
        <button type="button" data-val="150">150</button>
        <button type="button" data-val="custom">Autre…</button>
      </div>
      <input id="guestsInput" type="number" min="10" max="500" value="50" style="display:none">

      <div class="progress-bars">
        <!-- tes barres d’avancement existantes ici -->
      </div>
    </div>

    <!-- AVANCÉ -->
    <div id="avance" style="display:none">
      <p>Réglages détaillés du budget mariage…</p>
      <div class="progress-bars">
        <!-- barres avancées -->
      </div>
    </div>

    <!-- CTA EXPRESS -->
    <div id="ctaExpress" style="display:none; gap:.6rem; margin-top:.6rem">
      <a class="btn btn-premium" href="/packs.html">Outils Premium &amp; Packs (checklists, modèles)</a>
      <a class="btn btn-ghost" href="/affiliations.html">Sélection partenaires : robes, déco, faire-part →</a>
    </div>

    <!-- CTA AVANCÉ -->
    <div id="ctaAvance" style="display:none; gap:.6rem; margin-top:.6rem">
      <a class="btn btn-premium" href="/packs.html">Outils Premium &amp; Packs</a>
      <a class="btn btn-ghost" href="/affiliations.html">Sélection partenaires : robes, déco, faire-part →</a>
      <a class="btn btn-primary" href="/checklist.pdf" download>Télécharger la checklist PDF</a>
    </div>
  </section>

  <!-- SEO texte -->
  <section class="seo-text">
    <h2>Préparer son mariage avec sérénité</h2>
    <p>Notre simulateur champêtre vous permet d’estimer votre budget en quelques clics, selon vos envies et le nombre d’invités. Que vous soyez dans une organisation rapide (mode Express) ou un suivi détaillé (mode Avancé), vous disposez d’outils pratiques et de partenaires de confiance.</p>
    <p>Découvrez également nos checklists, modèles, et ressources premium pour ne rien oublier le jour J. Mariage bohème, chic, intime ou grandiose : adaptez votre budget en toute simplicité.</p>
  </section>

  <!-- Derniers articles -->
  <section class="card blog-preview">
    <h2 class="section-title">Derniers articles du blog</h2>

    <div style="text-align:center; margin: .5rem 0 1rem">
      <a href="/blog/" class="btn btn-primary">Voir tous les articles</a>
    </div>

    <div class="posts-grid">
      {% for post in collections.posts | reverse | slice(0,3) %}
        <a class="post-card" href="{{ post.url }}">
          {% if post.data.cover %}
            <img src="{{ post.data.cover }}" alt="{{ post.data.title }}">
          {% endif %}
          <h3>{{ post.data.title }}</h3>
          <p>{{ post.data.excerpt | safe }}</p>
        </a>
      {% endfor %}
    </div>
  </section>

</main>

<footer>
  <p>&copy; 2025 Mon Mariage Champêtre</p>
</footer>

<style>
  .blog-preview{margin-top:1rem}
  .posts-grid{display:grid;gap:1rem}
  @media(min-width:900px){.posts-grid{grid-template-columns:repeat(3,1fr)}}
  .post-card{display:flex;flex-direction:column;background:#fff;border:1px solid var(--line, #e7e2d9);border-radius:12px;overflow:hidden;text-decoration:none;color:inherit}
  .post-card img{width:100%;height:150px;object-fit:cover}
  .post-card h3{font-size:1rem;margin:.6rem .75rem;color:var(--brand, #3e5c49)}
  .post-card p{font-size:.9rem;color:var(--muted, #6f7b74);margin:0 .75rem .8rem}
</style>

<script>
(function(){
  var logo = document.querySelector('header h1');
  if(logo && logo.tagName!=='A'){
    logo.style.cursor='pointer';
    logo.addEventListener('click', function(){ location.href='/' });
  }
})();

// Pills invités
const pillsWrap = document.querySelector('#guestsPills');
const guestsField = document.querySelector('#guestsInput');
if (pillsWrap && guestsField) {
  pillsWrap.addEventListener('click', function(e){
    const b = e.target.closest('button[data-val]');
    if(!b) return;
    pillsWrap.querySelectorAll('button[data-val]').forEach(x=>x.classList.remove('active'));
    b.classList.add('active');
    const v = b.dataset.val;
    if(v==='custom'){
      guestsField.style.display='inline-block';
      guestsField.focus();
    }else{
      guestsField.style.display='none';
      guestsField.value = v;
    }
  });
  if(!guestsField.value) guestsField.value = 50;
}

// Switch Express / Avancé
const modeExpressBtn = document.getElementById('modeExpress');
const modeAvanceBtn  = document.getElementById('modeAvance');
const boxExpress     = document.getElementById('express');
const boxAvance      = document.getElementById('avance');
const ctaExpress     = document.getElementById('ctaExpress');
const ctaAvance      = document.getElementById('ctaAvance');

function setMode(which){
  const isExpress = which==='express';
  if(modeExpressBtn) modeExpressBtn.classList.toggle('active', isExpress);
  if(modeAvanceBtn)  modeAvanceBtn.classList.toggle('active', !isExpress);
  if(boxExpress)  boxExpress.style.display = isExpress ? '' : 'none';
  if(boxAvance)   boxAvance.style.display  = isExpress ? 'none' : '';
  if(ctaExpress)  ctaExpress.style.display = isExpress ? 'grid' : 'none';
  if(ctaAvance)   ctaAvance.style.display  = isExpress ? 'none' : 'grid';
}
if(modeExpressBtn) modeExpressBtn.addEventListener('click', ()=>setMode('express'));
if(modeAvanceBtn)  modeAvanceBtn .addEventListener('click', ()=>setMode('avance'));
setMode('express');
</script>

</body>
</html>
